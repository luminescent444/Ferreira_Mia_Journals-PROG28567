using UnityEngine;

public class RowSquares : MonoBehaviour
{

    /*Vector2 blCorner;
    Vector2 tlCorner;
    Vector2 brCorner;
    Vector2 trCorner;

    float squareY = -5;
    float squareX = -8;
    *//*public RowGeneration;*//*

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        *//*//figure out where the lines should be
        blCorner.x = (squareX - 1) + i * 2; blCorner.y = squareY - 1;
        brCorner.x = (squareX + 1) + i * 2; brCorner.y = squareY - 1;
        tlCorner.x = (squareX - 1) + i * 2; tlCorner.y = squareY + 1;
        trCorner.x = (squareX + 1) + i * 2; trCorner.y = squareY + 1;*/
    //}
}
